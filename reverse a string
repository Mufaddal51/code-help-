
#include <iostream>
using namespace std;

int
main ()
{
  char name[100];
  cout << "Before reversing" << endl;
  cin >> name;

  int i = 0;
  int n = getlength (name);
  int j = n - 1;
  while (i <= j)
	{
	  swap (name[i], name[j]);
	  i++;
	  j--;
	}

  cout << "after reverse->" << name;

}
