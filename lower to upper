#include <bits/stdc++.h>
using namespace std;
void
convertupper (char name[])
{
  int n = strlen (name);
  for (int i = 0; i < n; i++)
	{
	  name[i] = name[i] - 'a' + 'A';
	}
}

int
main ()
{
  char name[100] = "racecar";
  int n = strlen (name);

  convertupper (name);
  cout << name;

}
